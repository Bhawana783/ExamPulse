{
  "name": "exampulse",
  "private": true,
  "version": "1.0.0",
  "description": "ExamPulse root scripts",
  "scripts": {
    "dev": "concurrently -n backend,client -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix backend && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
